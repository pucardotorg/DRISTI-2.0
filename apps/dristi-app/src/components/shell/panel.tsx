/**
 * How a panel sits on a screen: the DS Card lifted by the DS dual-layer `shadow-raised`
 * and edged with `hairline` — the same vocabulary as the owner's demo (card + soft 5%
 * shadow) and the accepted advocate-home and e-filing builds. Apply to every `Card`.
 */
export const PANEL_CLASS = "border-hairline shadow-raised";
