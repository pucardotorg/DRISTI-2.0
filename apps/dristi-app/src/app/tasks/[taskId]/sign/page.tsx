import { TaskActPage } from "@/components/tasks/act/act-page";

/** Sign a document on a task — the signing flow's own page (v2.2). */
export default function Page() {
  return <TaskActPage action="sign" />;
}
