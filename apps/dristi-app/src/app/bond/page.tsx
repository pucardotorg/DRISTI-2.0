import { BondSigningScreen } from "@/components/filing/bond-signing-screen";

/**
 * The bail bond signing link — what the litigant and each surety receive by
 * SMS/email once the advocate e-signs and submits the bond.
 */
export default function Page() {
  return <BondSigningScreen />;
}
